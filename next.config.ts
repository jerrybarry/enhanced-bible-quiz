/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['https://zmrkwwcekblscdrfmldn.supabase.co'],
  },
}

module.exports = nextConfig

module.exports = {
  experimental: {
   // appDir: true,
  },
};

